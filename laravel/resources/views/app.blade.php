<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title inertia>{{ config('app.name', 'TroyWeb') }}</title>

        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.bunny.net">
        <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />
        <link rel="icon" sizes="192x192" href="{{ asset('/images/standard-ico.png') }}" type="image/png"/>
    <link rel="shortcut icon" href="{{ asset('/images/shortcut-favicon.png') }}" type="image/png"/>
    <link rel="apple-touch-icon" href="{{ asset('/images/touch-favicon.png') }}" type="image/png"/>

        <!-- Scripts -->
        @routes
        @vite(['resources/js/app.js', "resources/js/Pages/{$page['component']}.vue"])
        @inertiaHead
    </head>
    <body class="font-sans antialiased">
        @inertia
    </body>
</html>
